# Render Blueprint: deploy backend from repo root (Dashboard → New → Blueprint).
services:
  - type: web
    name: movie-booking-api
    runtime: java
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/movie-booking-1.0.0.jar
    plan: free
    healthCheckPath: /api/movies
